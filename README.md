To run project with following CLI command:
1. git pull:
2. npm install
3. npm run

The demo site is here:


Reusable components: imageContainer, descriptionContainer product container. They can be used in "Product page" and "ordersummary page".  
